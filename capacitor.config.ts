import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.merrypenguinswim',
  appName: 'merry-penguin-swim',
  webDir: 'dist'
};

export default config;
